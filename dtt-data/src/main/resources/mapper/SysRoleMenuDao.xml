<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.iceblue.data.dao.SysRoleMenuDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="cn.iceblue.core.pojo.entity.SysRoleMenuEntity" id="sysRoleMenuMap">
        <result property="id" column="id"/>
        <result property="roleId" column="role_id"/>
        <result property="menuId" column="menu_id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="revision" column="revision"/>
        <result property="createdBy" column="created_by"/>
        <result property="createdTime" column="created_time"/>
        <result property="updatedBy" column="updated_by"/>
        <result property="updatedTime" column="updated_time"/>
    </resultMap>

    <select id="selectByPage" resultType="cn.iceblue.core.pojo.entity.SysRoleMenuEntity">
        select * from dtt_sys_role_menu ${ew.customSqlSegment}
    </select>
    <select id="getMenuByRoleId" resultType="cn.iceblue.core.pojo.entity.SysMenuEntity" parameterType="list">
        SELECT  dsm.* FROm  dtt_sys_role_menu dsrm
              INNER JOIN dtt_sys_menu dsm  on dsrm.menu_id = dsm.id
        where
        <foreach collection="list" open="role_id in(" close=")" item="id" separator=",">
            #{id}
        </foreach>
    </select>


</mapper>